1)
mkdir nomDepotLocal
cd nomDepotLocal
git init
vim readme.txt
git add readme.txt
git commit -m "readme contenant mon nom"
git remote add origin "adresse de mon depot git"
git push -u origin master

2)
git pull
vim readme.txt
git add readme.txt
git commit -m "ajout du sigle du cours"
git push

3)
cd ..
rm rf nomDepotLocal
git clone "adresse de mon depot git"

4)
git pull
vim FizzBuzz.java
javac FizzBuzz.java //pour compiler
java FizzBuzz 15 //agrs[0]=15 || java FizzBuzz //sans arguments

5)
git pull
vim .gitignore
git add .
git commit -m "ajout du programme FizzBuzz"
git push

6)
Remplacer 100 par 200 dans mon ancien programme
git add .
git commit -m "modification de ma borne superieure"

 
RQ :
Quelques commandes git utiles:

git commit -a = git add . + git commit . //Ajoute toutes mes modifications
à l'index et fait un commit pour tout

git commit --amend //permet de remplacer le message du dernier commit, ça marche ssi je
n'ai pas encore push mes modifications

git revert //ne supprime pas mon ancien commit et ne l'annule pas, mais ajoute un commit aui
supprime les modifications du commit précédent.




